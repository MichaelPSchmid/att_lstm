# Aufräumen: Dateien evaluieren - 2026-01-21

## Ziel

Verzeichnis für Verzeichnis durchgehen und evaluieren, was noch relevant ist.

## Fortschritt

| # | Verzeichnis | Status | Notizen |
|---|-------------|--------|---------|
| 1 | `/` (Root-Dateien) | ✅ | 2 gelöscht, 3 später verschieben |
| 2 | `attention_visualization/` | ✅ | Komplett gelöscht (veraltet) |
| 3 | `comparison/` | ✅ | Komplett gelöscht (alte pickle-Version) |
| 4 | `config/` | ✅ | Behalten (aktuelles Config-System) |
| 5 | `data/` | ✅ | Behalten (Dataset + Readme) |
| 6 | `docs/` | ✅ | Später aktualisieren (alte Referenzen) |
| 7 | `evaluation/` | ✅ | Komplett gelöscht (alte Pickle-Ergebnisse) |
| 8 | `lightning_logs/` | ✅ | Behalten (archiv + aktuelles Training) |
| 9 | `model/` | ✅ | 4 alte Dateien gelöscht, Struktur später verbessern |
| 10 | `optuna/` | ✅ | Behalten (aktuell nicht verwendet, evtl. später) |
| 11 | `plot/` | ✅ | Später refactoren (Skripte anpassen) |
| 12 | `prepared_dataset/` | ✅ | Komplett gelöscht (alte Pickle, window=100) |
| 13 | `preprocess/` | ✅ | Später refactoren (mehrere Varianten konsolidieren) |
| 14 | `results/` | ✅ | Behalten (leer, für neue Ergebnisse) |
| 15 | `scripts/` | ✅ | Behalten (aktive Skripte) |

**Übersprungen:** `.git/`, `.venv/`, `__pycache__/`, `.claude/` (automatisch generiert)

## Aktionen

### Zu löschen
- [x] `nul` (Windows-Artefakt)
- [x] `model_comparison_no_interpolation.pdf` (veraltet)

### Später verschieben
- [x] `config.py` → `config/settings.py` ✅
- [x] `config_loader.py` → `config/loader.py` ✅
- [x] `data_module.py` → `model/data_module.py` ✅
- Imports angepasst in 14 Dateien (scripts/, optuna/, plot/, preprocess/)

### Später aktualisieren (Dokumentation)
- [x] `docs/README.md` - Projektstruktur, Schnellstart ✅
- [x] `docs/configuration.md` - Pfade, Verzeichnisstruktur, Ausführung ✅
- [x] `docs/data_pipeline.md` - preprocess_parallel.py, inspect_dataset.py ✅
- [x] `docs/evaluation.md` - evaluate_model.py, Attention Visualisierung ✅
- [x] `docs/training.md` - train_model.py, YAML-Konfiguration, Seed 42 ✅

### Später umstrukturieren
- [x] `model/` - flache Struktur mit konsistenten Namen ✅
  - `LSTM.py` → `lstm_baseline.py`
  - `LSTM_attention.py` → `lstm_simple_attention.py`
  - `diff_attention/additive_attention.py` → `lstm_additive_attention.py`
  - `diff_attention/scaled_dot_product.py` → `lstm_scaled_dp_attention.py`
  - `diff_attention/` Ordner gelöscht
- [x] `plot/` - aufgeräumt ✅
  - `HYUNDAI_SONATA_2020/` mit 5 alten PNGs gelöscht
  - `plot_steer[-1,1].py` + `plot_steer[-0.25,0.25].py` → `plot_steer_distribution.py`
  - Neues Skript: konfigurierbar via CLI (--range, --vehicle, --num-csvs)
- [x] `preprocess/` - konsolidiert ✅
  - Gelöscht: `slice_window.py`, `slice_window_20999.py`, `slice_window_nF.py`, `data_preprocessing.py`
  - `random_sample.py` → `inspect_dataset.py` (mit CLI-Parametern)
  - Nur noch: `preprocess_parallel.py` (Haupt-Skript) + `inspect_dataset.py` (Debug)

### Zu archivieren
- (wird während Evaluation gefüllt)

### Zu behalten
- (wird während Evaluation gefüllt)

## Entscheidungen

- **2026-01-21:** Dateien verschieben mit expliziten Imports (best practice) statt Re-exports in `__init__.py`
  - `from config.settings import X` statt `from config import X`
  - `from config.loader import X` statt `from config_loader import X`
  - `from model.data_module import X` statt `from data_module import X`
